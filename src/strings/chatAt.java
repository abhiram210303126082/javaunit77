public class chatAt {
    public static void main(String[] args) {
        String str = "Hi this is abhiram";
        int index = 5;

        // Get the character at a specific index
        char character = str.charAt(index);
        System.out.println("Character at index " + index + " is: " + character);
    }
}
